  # Cross-family adversarial review: `scratch/mc-radius3-theorem.md`

  Reviewer: Kimi K3 (first non-Claude-family). Method: complete paper-level re-derivation from the document alone, plus a fresh machine battery (`/tmp/kimi/mc-review/fresh_review.py` — my own SAT oracle, my own ISLAND brute-forcer, my own structural auditor; the graph-level oracle `independent_mc.py` re-validated by me 400/0 against exhaustive 2^n enumeration and its pruning logic audited by hand: pruning on ">1 cross among coloured neighbours" is sound since colouring more vertices never removes a cross).

  ## Findings

  1. **§3 (bipartite, connected, radius exactly 3) — HOLDS.** Attacked all four eccentricity cases for all n,m ≥ 1: L3 vertices sit at distance ≥ 3 from u (neighbours all in L2, none adjacent to u); L1 vertices have ≤ 3 L2-neighbours < |L2| ≥ 4; L2 vertices have ≤ 2 L1-neighbours < |L1| ≥ 5; ecc(u) = 3 gives radius ≤ 3. Degree claims re-verified per vertex type, including the degenerate cases: a copy `c` always has degree 2 (base ≠ proxy, since bases are t_i/f_i and proxies are distinct vertices), `z_C` always has degree 3 (proxies are per-position, distinct even under repeated literals). Fresh machine audit (bipartite, connected, radius == 3, |V| = 6n+19m+12, every edge joins consecutive BFS layers) on 165 instances including handcrafted degenerates: 0 violations.

  2. **Claim 4.2 (case A0 impossible) — HOLDS.** The whole case rests on "every L3 vertex has degree ≥ 2." Verified g_i = 3, c = 2, z_C = 3, Zk = 2+|BW| ≥ 7 for n,m ≥ 1, and machine-checked on all 165 instances. Blue ⊆ L3 with a degree-≥2 blue vertex forces ≥ 2 crosses. No escape.

  3. **Claim 4.4 (case B impossible) — HOLDS.** Attacked branch exhaustiveness against the L1 inventory: BW ∪ {w'_W} ∪ {4 ballast witnesses} — the three branches cover it exactly. Attacked each forcing chain for an escape: b = W or b = w'_W forces G_W blue → its budget consumed by the red other witness → Zk blue → Zk crosses T1 and T2 (both red, their ballast witnesses ≠ b, pinned by Claim 4.3) — already 2 crosses. b ballast: forces that T blue → Zk blue → crosses the other T plus any guard, and G_{w_a} always exists. No branch has an out; Claim 4.3 does the pinning work in every branch. Prior reviews' exhaustive-over-every-b machine checks concur; my paper-level re-derivation independently concurs.

  4. **WLOG u red — HOLDS.** Colour swap is an involution on valid colourings; existence is colour-symmetric. Trivial and correct.

  5. **Lemma B' — SUSPICIOUS (prose gap only; could not convert to a counterexample).** In (⇒), the step "budget forces exactly one L1-neighbour, so B2 ⊆ S" silently uses that every L2 vertex has *at least one* L1-neighbour. The budget argument alone only gives ≤ 1. The missing premise is true — every vertex in BFS layer 2 has a layer-1 neighbour by definition of BFS — and I machine-verified it on every battery instance, so it is an omission, not a hole. Everything else re-derived cleanly: (α) from witness budget, (β') from blue z's crosses being exactly N(z) ∖ B2, (γ') from red v's blue-L3 neighbourhood; (⇐) all six vertex classes budget-check, including the tight "z ∈ L3 ∖ N(B2) ⇒ 0 crosses" case.

  6. **Lemma D (bridge) — HOLDS.** Attacked the set computation directly: my auditor rebuilds S from the graph (L2 vertices with exactly one L1-neighbour) and compares against {a} ∪ {t_i, f_i} ∪ {proxies} — exact match on all 165 instances. Guards and ballast correctly excluded (two witnesses each). Zk is unreachable from S (its L2-neighbours are guards/ballast only), every base L3 vertex has all L2-neighbours in S, and (γ') is vacuous on L2 ∖ S since N(v) ∩ L3 = {Zk} ∉ N(B2). The reduction of (β'), (γ') to ISLAND's (β), (γ) is exact.

  7. **Lemma A — HOLDS.** Attacked both directions with the degenerate seeds the brief names. (⇒): the assignment-induced B checks out, including unused variables (an unselected literal with zero occurrences sees only its gate) and repeated literals (proxies per-position). (⇒-direction steps): proxy⇒base via the two copies + (γ); base⇒a via the gate + (β); B≠∅⇒a∈B; a∈B⇒full assignment — this step works for clause-absent variables because g_i exists for every i regardless of clause presence; base⇔proxies; then t_C ∈ {0,2,3}. Fresh exhaustive ISLAND brute-forcer vs fresh exhaustive SAT: agreement on all 165 instances, including 4 UNSAT.

  8. **§1 source NP-completeness — HOLDS.** My own Schaefer check, independent of the document's machine verification: escapes 0-valid ((¬a,b,c) fails at 000), 1-valid ((a,¬b,¬c) fails at 111), Horn ((0,1,1)∧(1,0,1) = (0,0,1) ∉ R), dual-Horn (OR-closure fails on the (¬,¬,¬) pattern), affine (|R| = 5, not a power of 2), bijunctive (maj((1,1,0),(1,0,1),(0,0,0)) = (1,0,0) ∉ R). The n,m ≥ 1 restriction is handled correctly: m=0/n=0 instances are always satisfiable and map to the fixed satisfiable instance (x,x,x) — no NO-instance needed since the reduction is defined exactly on the restricted class.

  9. **End-to-end machine cross-check — HOLDS, with a stated instrument limit.** Oracle validation 400/0; handcrafted battery (15 degenerate formulas: (x,x,x), (¬x,¬x,¬x), (x,¬x,x), forced-value clauses, two handcrafted UNSAT pairs, unused variables, duplicate clauses, all-negative, a 4-clause UNSAT forbidding every assignment): every completed check agrees, sat = island = matching-cut. Random battery: 150 formulas, 0 mismatches. Honest limit: 28 graph-level runs timed out at |V| ≥ 75 (recorded as unknown, never as a result) — but the exhaustive set-level Lemma A check agreed with SAT on every one of those instances too. This matches the document's own §9 ceiling and does not weaken the proof, only the testing.

  10. **Silent assumptions hunted.** Found one (finding 5). Otherwise: the case split's "both colours used" is stated; connectedness of the target class is proven, not assumed; simplicity of G(F) holds (all vertex-name families disjoint by prefix); polynomial-time constructibility is linear and correct.

  ## Verdict

  **The document contains a correct proof of the theorem as stated.** I could not refute any claim at paper level or machine level. The only finding is finding 5 — a true-but-unstated BFS premise in Lemma B' (⇒), a one-sentence prose repair, not a mathematical hole. Both directions of every lemma re-derive, the case analysis is exhaustive, the degenerate seeds (repeated literals, unused variables, n=1, duplicate clauses, mixed signs) all behave as the proof requires, and a fresh independent machine battery targeting exactly those seeds agrees at every level the instruments reach.

